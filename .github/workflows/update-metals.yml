name: "Update Metals"

on:
  pull_request:
  schedule:
  - cron: "0 8 * * *"

jobs:
  fetcher:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: "Install Nix ❄️"
        uses: cachix/install-nix-action@v18

      - name: "Install Cachix ❄️"
        uses: cachix/cachix-action@v12
        with:
          name: gvolpe-nixos
          authToken: "${{ secrets.CACHIX_AUTH_TOKEN }}"

      - name: "Fetch latest Metals"
        id: fetcher
        run: nix run .#metals-updater

      - name: "Set Metals Version for commit-message 🤷"
        shell: bash
        run: echo 'METALS_VERSION=${{ steps.fetcher.outputs.metals_version }}' >> $GITHUB_ENV

      - name: "Create pull request"
        uses: peter-evans/create-pull-request@v4.2.3
        with:
          committer: GitHub <noreply@github.com>
          author: ${{ github.actor }} <${{ github.actor }}@users.noreply.github.com>
          token: ${{ secrets.REPO_GITHUB_TOKEN }}
          base: master
          branch: update/metals-latest-snapshot
          delete-branch: true
          commit-message: 'Update Metals to $METALS_VERSION'
          title: 'Update Metals to ${{ steps.fetcher.outputs.metals_version }}'
          body: |
            Update Metals to version `${{ steps.fetcher.outputs.metals_version }}`. See https://scalameta.org/metals/latests.json
          labels: |
            metals-update
